check_distributions:
	rm -rf autogenerated.*
	R --slave < check_distributions.R

matrix_arrays:
	R --slave < matrix_arrays.R

report:
	R --slave < report.R

test_reload:
	R --slave < test_reload.R

nan_error_ex:
	echo "TMB:::gdbsource('nan_error_ex.R')" | R --vanilla

clean :
	rm -rf *.o *.so *.dll *~ core *.output.RData *.pdf *.profile
	rm -rf */*.o */*.so */*.dll */*~ core */*.output.RData */*.pdf */*.profile
